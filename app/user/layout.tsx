import { UserSideBar } from "@/components/layout/user/sidebar";

const UserLayout = () => {
  return (
    <main className="relative pl-[280px]">
      <UserSideBar />
    </main>
  );
};

export default UserLayout;
