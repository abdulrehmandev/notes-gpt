import { atom } from "jotai";

export const sideBarAtom = atom(false);
export const searchCommandAtom = atom(false);

export const findSheetAtom = atom(false);
export const userSideBarAtom = atom(false);
